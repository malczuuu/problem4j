plugins {
    // net.nemerosa.versioning plugin in version 2.8.2 seems to be broken
    id "net.nemerosa.versioning" version "2.7.1"
}

subprojects {

    group = "io.github.malczuuu.problem4j"
    version = versioning.info.tag ?: versioning.info.commit.substring(0, 10)
}