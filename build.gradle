plugins {
    id "net.nemerosa.versioning" version "2.8.2" apply false
}

subprojects {
    apply plugin: "net.nemerosa.versioning"

    group = "pl.malczuuu.problem4j"
    version = versioning.info.tag ?: versioning.info.commit.substring(0, 10) + (versioning.info.dirty ? "-dirty" : "")
}